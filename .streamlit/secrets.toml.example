# Example secrets file - not used by this app but available for future API keys
# Copy this to secrets.toml if you need to add API keys

# [api]
# binance_key = "your_binance_api_key_here"
# coindesk_key = "your_coindesk_api_key_here"

# Note: This app currently uses only public APIs that don't require authentication
